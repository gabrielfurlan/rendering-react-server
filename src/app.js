//Modules Node
const React = require('react');
const ReactDOM = require('react-dom');

//Components
const Component = require('./component/Component.jsx');		

ReactDOM.render(
	React.createElement(Component), document
);